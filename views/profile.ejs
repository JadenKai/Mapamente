<%- include('partials/header') %>
        <header class = "pageTitle">
            Hey <%= user.username %>!
        </header>
        <div style="display:flex; margin:20px; gap:20px;">
            <div style="flex:2;">
                <div class="center">
                    <img class="profilePicture" src="<%= locals.profilePicSrc || '/images/Profile.png' %>" alt="Profile">
                </div>
                <br>
                <div class="center">
                    <button id="photoUpload" class="signUpButton">
                        Upload new photo
                    </button>
                </div>
                    <form id="photoForm" action="/profile/photo" method="POST" enctype="multipart/form-data">
                    <input type="file" id="photoUploadInput" name="photo" accept="image/*" style="display:none;">
                </form>
            </div>
            <div style="flex:4;">
                <p>
                    Stats:
                    <!-- Stats from quiz scores -->
                     <table class="statsTable">
                        <thead>
                            <tr>
                                <th colspan="2" style="padding:5px;">Overall Stats</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td style="padding:5px;">Madrid</td>
                                <td style="padding:5px;">1200 pts</td>
                            </tr>
                            <tr>
                                <td style="padding:5px;">Austin</td>
                                <td style="padding:5px;">1000 pts</td>
                            </tr>
                        </tbody>
                     </table>
                </p>
                <p>
                    <br><br><br><br><br><br><br><br><br>
                    Settings:<br>
                    <ul>
                        <li>Toggle light/dark mode</li>
                        <li>Toggle public/private mode</li>
                        <li>Change password</li>
                        <li>Change profile picture</li>
                    </ul>
                </p>
            </div>
        </div>
<script>
  document.getElementById('photoUpload').addEventListener('click', function() {
    document.getElementById('photoUploadInput').click();
  });
  document.getElementById('photoUploadInput').addEventListener('change', function() {
    document.getElementById('photoForm').submit();
  });
</script>
<%- include('partials/footer') %>
